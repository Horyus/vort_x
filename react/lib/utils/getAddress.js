import { utils } from 'ethers';
/**
 * @description Utility to format addresses
 *
 * @param address Value to format
 */
export var getAddress = function (address) {
    return utils.getAddress(address);
};
//# sourceMappingURL=getAddress.js.map